- platform: history_stats
  name: master_bedroom_baseboard_heat_on_time_24
  entity_id: binary_sensor.master_bedroom_baseboard_heat_on
  state: 'on'
  type: time
  duration:
    hours: 24
  end: '{{ now() }}'

- platform: history_stats
  name: master_bedroom_baseboard_heat_on_time_yesterday
  entity_id: binary_sensor.master_bedroom_baseboard_heat_on
  state: 'on'
  type: time
  duration:
    hours: 24
  end: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'

- platform: history_stats
  name: master_bedroom_baseboard_heat_on_time_week
  entity_id: binary_sensor.master_bedroom_baseboard_heat_on
  state: 'on'
  type: time
  duration:
    days: 7
  end: '{{ now() }}'
